name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # Apple Silicon + Windows. Intel macOS is unsupported for now: GitHub
          # retired its Intel (macos-13) runners, and the autotools sys-crates
          # (opus/lame/vorbis) emit host-arch objects, so the x86_64 slice cannot
          # be cross-compiled from arm64 either. Revisit with vendored cmake
          # builds if Intel demand appears.
          - platform: macos-latest
            target: ""
            args: ""
          - platform: windows-latest
            target: ""
            args: ""
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: Install macOS build tools (autotools for the Opus sys-crate)
        if: startsWith(matrix.platform, 'macos')
        run: brew install autoconf automake libtool

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - uses: swatinem/rust-cache@v2
        with:
          key: ${{ matrix.platform }}-${{ matrix.target }}

      - run: npm ci

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "Waver ${{ github.ref_name }}"
          releaseBody: "See the commit history for changes."
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}

  checks:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install macOS build tools (autotools for the Opus sys-crate)
        run: brew install autoconf automake libtool
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
      - run: npm ci
      - run: npm run typecheck
      - run: cargo test --workspace
